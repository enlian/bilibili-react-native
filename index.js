/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Example from './view/tabView';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Example);
